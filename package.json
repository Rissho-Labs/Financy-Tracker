{
  "name": "finance-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Finance Tracker — interface web + shell Android (Capacitor)",
  "scripts": {
    "www:prepare": "node scripts/prepare-www.mjs",
    "setup:deepseek": "node scripts/setup-deepseek-config.mjs",
    "deploy:rules": "npm run www:prepare && npx firebase-tools deploy --only firestore:rules,storage",
    "deploy:hosting": "npm run www:prepare && npx firebase-tools deploy --only hosting",
    "deploy:functions": "npx firebase-tools deploy --only functions",
    "deploy:firebase": "npm run www:prepare && npx firebase-tools deploy --only firestore:rules,storage,hosting,functions",
    "start": "node scripts/start-dev.mjs",
    "serve": "serve www -l 5050 --no-etag",
    "cap:sync": "npm run www:prepare && cap sync",
    "cap:open": "cap open android",
    "android:sha1": "powershell -ExecutionPolicy Bypass -File scripts/print-android-sha1.ps1"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^8.2.0",
    "@capacitor/android": "^8.3.2",
    "@capacitor/core": "^8.3.2",
    "@capgo/capacitor-native-biometric": "^8.4.2",
    "@pantrist/capacitor-plugin-ml-kit-text-recognition": "^8.0.0",
    "firebase": "^12.13.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.2",
    "esbuild": "^0.25.0",
    "serve": "^14.2.5"
  }
}
