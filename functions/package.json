{
  "name": "financy-functions",
  "description": "Firebase Cloud Functions — Financy Tracker",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "22" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  }
}
